class UrlManager(object):
    """定义一个类，"""

    @staticmethod  # 静态方法
    def buildUrl(path):
        """定义连接地址"""
        return path

    @staticmethod
    def buildStaticUrl(path):
        return path
